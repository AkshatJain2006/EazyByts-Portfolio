# Portfolio CMS

## Setup

Backend:
```bash
cd backend
npm install
npm start
```

Frontend:
```bash
cd frontend  
npm install
npm start
```

Create `.env` in backend:
```
PORT=3001
JWT_SECRET=your-secret
MONGODB_URI=your-mongodb-uri
```